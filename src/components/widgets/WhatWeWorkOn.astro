---
let hammersquiggle = await import('~/assets/images/hammersquiggle.svg')

const items = [
	[
		{
			title: 'Embedded Software Development',
			description:
				`
We are serious about firmware. At the end of the day you need binaries you can trust.
We will enable your product's transition to mass production and beyond, making sure your product can adapt at the speed of the market.
				`,
		},
	],
	[
		{
			title: 'Automated Testing',
			description:
				`We provide automated testing solutions for businesses of all sizes. From triggering all the way to report delivery.
				Your cloud or our cloud.  Your lab or our lab.  With us, you will see the value of testing, not the cost.  Guaranteed`
				,
		},
	],
];
---

<section class="scroll-mt-16" id="features">
	<div class="px-4 py-16 mx-auto max-w-6xl ">
		<div class=" mb-10 flex flex-row ">
            <div class="mb-4 mr-12">
                <image src={hammersquiggle.default} class="w-24 h-24" />
            </div>
            <div class="mb-4">
                <h2 class="text-[#F5F5F5] text-6xl font-bold mb-4 font-heading">
                    What We Work On 
                </h2>
            </div>
		</div>
		<div class="grid mx-auto space-y-6 md:grid-cols-2 md:space-y-0">
			{
				items.map((subitems) => (
					<div class="space-y-8 sm:px-8">
						{subitems.map(({ title, description }) => (
							<div class="flex flex-row max-w-md">
								<div>
									<h3 class="mb-3 text-[#dfdfdf] text-2xl font-bold">{title}</h3>
									<p class=" text-gray-500" >{description}</p>
								</div>
							</div>
						))}
					</div>
				))
			}
		</div>
	</div>
</section>
